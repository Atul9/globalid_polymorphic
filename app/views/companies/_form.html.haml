= simple_form_for @company, html: { class: 'form-horizontal'} do |f|
  .fields
    .container
      .row
        .col-md-2
          = f.input :name
          %br
          = f.button :submit, class: 'btn btn-primary'


